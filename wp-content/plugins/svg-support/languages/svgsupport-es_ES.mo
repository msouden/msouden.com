Þ•    &      L      |      |  4   }  p   ²     #     ,     G     [     l     „     ‘       
   ª  ß   µ     •     °     Ð  $   Ü       "         C     P  %   Y       ³     ¼   S       =     I  Î     	     	  Ž   ,	     »	  +   ¿	  K   ë	     7
     Q
     V
     j
  ­  m
  ;     }   W     Õ  )   Þ               *     =     J     Z     h  Í   {      I      j     ‹  $   —      ¼  '   Ý            *   $  &   O  »   v  Ó   2  r     4   y    ®     É     Í  •   Ý     s  #   v  _        ú               +    Restricts SVG upload priveledges to Administrators. Allow SVG file uploads using the WordPress Media Library uploader plus direct styling of SVG elements using CSS. Benbodhi Buy Benbodhi a Beer &rarr; CSS Class to target Donate to author Extremely Simple To Use For example: Get Support Introduction Need help? Now, embed your SVG image like a standard image with the addition of adding the class <code>style-svg</code> (or your custom class from above) to any IMG tags that you want this plugin to swap out with your actual SVG code. Restrict to Administrators Restrict to Administrators only SVG Support SVG Support Options and Instructions SVG Support Settings and Usage SVG Support for your media library Save Changes Settings Style SVG elements directly using CSS The WordPress Plugin Repository The default target class is <code>style-svg</code>. You can change it to your own class such as <code>my-class</code> by typing it here.<br>Leave blank to use the default class. The main purpose of this is to allow styling of SVG elements. Usually your styling options are restricted when using <code>embed</code>, <code>object</code> or <code>img</code> tags alone. The whole IMG tag element will now be dynamically replaced by the actual code of your SVG, making the inner content targetable. This allows you to target elements within your SVG using CSS. This plugin not only provides SVG Support like the name says, it also allows you to easily embed your full SVG file's code using a simple IMG tag. By adding the class <code>style-svg</code> to your IMG elements, this plugin dynamically replaces any IMG elements containing the <code>style-svg</code> class with your complete SVG. Usage Visit Support When using SVG images on your WordPress site, it can be hard to style elements within the SVG using CSS. <strong>Now you can, easily!</strong>